<ui:FluentWindow
    x:Class="SplitVpn.App.Views.SignInWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    Title="Вход"
    Width="920"
    Height="780"
    MinWidth="480"
    MinHeight="400"
    ExtendsContentIntoTitleBar="True"
    Icon="pack://application:,,,/Assets/app.ico"
    WindowBackdropType="None"
    WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar x:Name="TitleBar" Grid.Row="0" ShowHelp="False" ShowMaximize="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/app.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <StackPanel Grid.Row="1" Margin="16,0,16,8">
            <TextBlock x:Name="Hint" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Text="Войдите на странице вашей организации. Окно закроется само, когда шлюз примет вход." />
            <ui:InfoBar x:Name="Problem" Margin="0,8,0,0" IsClosable="False" Severity="Error" IsOpen="False"
                        AutomationProperties.LiveSetting="Assertive" />
        </StackPanel>
        <wv2:WebView2 x:Name="Browser" Grid.Row="2" AutomationProperties.Name="Страница входа" />
    </Grid>
</ui:FluentWindow>
